server:
  servlet:
    session:
      timeout: 168h
      cookie:
        name: lila-session
        http-only: true
        secure: true
  ssl:
    enabled: false
    key-store:
    store-type:
    key-store-password:
  port: 15342

spring:
  thymeleaf:
    cache: true
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false

lila:
  config:
